<!--<ul>
    {{#each server in controller}}
    <li>{{server.hostUrl}} - {{#linkTo "servers.edit" server}}edit{{/linkTo}} - <button {{action "deleteServer" server}}>delete</button> - ({{server.versionMayor}}.{{server.versionMinor}}) {{server.version}}</li>
    {{/each}}
</ul>-->

<div class=" warning">
    <span class="warning-icon"></span>
    <p>Please add a server before continuing.</p>
</div>
