<ul>
    {{#each server in controller}}
    <li>{{server.hostUrl}} - {{#linkTo "servers.edit" server}}edit{{/linkTo}} - <button {{action "deleteServer" server}}>delete</button> </li>
    {{/each}}
</ul>