/**
  This view handles the modal for adding a new Host

  @class AddHostView
  @extends App.ModalBodyView
  @namespace App
  @module App
**/

App.AddHostView = App.ModalBodyView.extend({
    templateName: 'modal/addHost',
    title: 'addHost'
});